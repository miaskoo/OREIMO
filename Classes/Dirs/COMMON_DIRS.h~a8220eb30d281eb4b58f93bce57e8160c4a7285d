#pragma once
#include <string>
#include <map>
using namespace std;

extern const string SPRITE_BLINK_DIR;
extern const string SPRITE_SPEAK_DIR;
extern const string NUMSPRITE_ONE_DIR;
extern const string NUMSPRITE_TWO_DIR;
extern const string NUMSPRITE_THREE_DIR;
extern const string NUMFACE_FOUR_DIR;
extern const string NUMFACE_FIVE_DIR;
extern const string NUMFACE_SIX_DIR;
extern const string NUMFACE_SEVEN_DIR;
extern const string NUMFACE_EIGHT_DIR;
extern const string NUMFACE_NINE_DIR;
extern const string NUMFACE_TEN_DIR;
extern const string NUMFACE_ELEVEN_DIR;
extern const string NUMFACE_TWELWE_DIR;
extern const string NUMFACE_THIRTEEN_DIR;
extern const string NUMFACE_FOURTEEN_DIR;
extern const string NUMFACE_FIVETEEN_DIR;
extern const string NUMFACE_SIXTEEN_DIR;
extern const string NUMFACE_SEVENTEEN_DIR;
extern const string NUMFACE_EIGHTTEEN_DIR;
extern const string NUMFACE_NINETEEN_DIR;
extern const string NAME_MAP_SPRITE;
extern const string NAME_MAP_FACE;
extern const string SPRITE_BLINK_ANIM_DIR[3];//имена для картинок анимации моргания везде одни
extern const string SPRITE_SPEAK_ANIM_DIR[3];//имена для картинок анимации говорения везде одни

extern map<string, map<string, map<string, string>>> SPRITE_AY_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_DA_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_KI_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_AK_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_KA_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_KU_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_MA_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_RO_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_SA_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_SE_DIR;
extern map<string, map<string, map<string, string>>> SPRITE_YO_DIR;